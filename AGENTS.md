# NOVA 超级桌面 AI 编码规范

## 产品边界

- NOVA 是承载多个创意工具的超级桌面；每个工具必须作为独立窗口接入，不得把工具能力直接耦合到桌面外壳。
- 内置工具与游戏均为本地优先应用。未经用户明确要求，不新增账号、云同步、远端持久化服务或不可见后台任务；同源静态资源只能由用户打开对应功能后按需请求。
- 桌面外壳负责窗口、Dock、全局状态和视觉环境；工具内部负责自己的编辑状态、历史记录和导出。

## 视觉语言

- 桌面遵循 Windows 的空间逻辑：应用与文件以桌面图标存在，底部任务栏负责启动与恢复，开始菜单集中展示固定应用；不得把桌面做成宣传首页或仪表盘。
- 窗口使用右上角最小化、最大化、关闭控制，保持聚焦层级和克制的半透明材质。
- 优先使用 CSS、文字与现有素材，不引入无必要的图片、SVG 或图标依赖。
- 新工具沿用桌面窗口结构、44px 窗口栏、圆角、玻璃边框和 Dock 唤起方式。
- 动画只用于状态变化和空间层级，必须支持 `prefers-reduced-motion`。

## 工具接入契约

- 工具组件不得依赖桌面的窗口位置与尺寸；根节点使用 `height: 100%` 并自行处理内部布局。
- 每个工具至少提供：打开、关闭、最小化、最大化、可识别名称和无障碍标签。
- 刷新桌面时不自动打开工具；用户必须从桌面入口或 Dock 主动启动。
- 关闭表示退出工具并移除 Dock 运行指示；最小化只隐藏窗口，工具保持运行，点击 Dock 图标恢复窗口。
- 工具状态留在工具组件内部；桌面只保存窗口可见性和窗口模式。
- 不允许工具阻断桌面的全局滚动、键盘或指针事件，除非交互明确发生在该工具画布中。

## 本地文件模型

- 桌面文件统一使用 `DesktopItem`，类型限定为文件夹、文本和图片；桌面、记事本、照片查看器与照片实验室共享同一数据源。
- 用户明确要求设备本地保存，因此桌面文件统一存入 IndexedDB 的 `nova-desktop`；旧版 `localStorage` 键 `nova-desktop-items` 仅用于一次性迁移，迁移成功后删除，不得同时保留第二套缓存或引入远端存储。
- 文本编辑实时更新桌面文件；照片实验室的完成操作必须生成图片文件并写回桌面；应用不得复制一份脱离桌面的私有文件状态。
- 刷新只恢复文件，不自动打开应用窗口。

## 资源与更新生命周期

- 应用和游戏统一通过 `lazyApps.ts` 的动态导入接入；桌面启动不得提前加载未打开窗口的业务模块。
- Service Worker 安装阶段只缓存桌面最小启动壳，不递归发现或预取懒加载模块、书籍、照片、游戏引擎与大型游戏资源。
- 可重新下载的代码和媒体必须按资源包写入独立 Cache Storage；资源包的分类、统计和删除协议统一由 Service Worker 管理，不在单个应用中自行维护缓存。
- 设置页必须把“用户数据”和“按需资源”分开展示：用户数据删除后不可恢复；按需资源可以删除，并在下次打开对应功能时重新下载。
- 清理资源缓存不得删除桌面文件、应用设置、阅读进度或游戏存档；清理某类用户数据时必须同时覆盖该应用使用的 localStorage 与 IndexedDB。
- 大型静态子应用必须拥有独立资源包，禁止进入系统壳缓存；关闭窗口只结束运行状态，不自动删除已下载资源。
- 发布新版本时更新 Service Worker 版本号。新 worker 安装完成后保持等待状态，由桌面显示更新提示，用户确认后再激活并刷新。
- Service Worker 只缓存同源成功响应；删除旧版本缓存时只能匹配 `nova-pwa-` 命名空间。

## 编码规则

- 优先在现有组件和样式体系内做最小修改，不做无关重构。
- React 状态必须有唯一职责；派生状态通过计算获得，避免重复保存。
- 所有可点击圆形图标必须有 `aria-label`；纯装饰元素保持不可交互。
- 不增加静默兜底、自动重试、额外校验或兼容分支；需要扩大行为时先取得用户确认。
- 默认不运行 lint 或完整构建；完成实现后运行 TypeScript 检查、相关单测，并检查本次 diff 是否超出需求。仅在用户明确要求时运行构建。

## 受控自动演进

每次迭代只执行一个明确目标，按以下闭环进行：

1. 观察：记录一个可复现的问题或一个用户明确提出的目标。
2. 假设：写出最小可验证改动和不修改的边界。
3. 实现：只修改完成该目标所需的文件。
4. 验证：运行 TypeScript 检查、相关单测，并按风险验证关键交互；仅在用户明确要求时运行构建。
5. 审计：检查 diff，移除顺手优化、隐式兜底和无关重构。
6. 记录：在交付说明中列出结果、验证项和仍未实现的能力。

- 单次自动演进最多进行三轮“实现—验证”循环；仍未解决时停止并向用户说明阻塞点。
- 不进行无人值守的无限循环，不自行发布，不修改用户文件与外部数据，不把视觉偏好扩展为新产品功能。
