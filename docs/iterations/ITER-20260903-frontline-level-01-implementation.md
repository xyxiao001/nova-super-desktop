# 前线第一关战斗实现

- 日期：2026-09-03
- 状态：正式战斗切片已可运行，整关复刻未验收
- 对应 Spec：`SPEC-20260903-frontline-faithful-battle.md`
- 关卡：`100101` / `desert-1` / `烈日沙漠1`

## 本轮目标

以第一关资源审计结果为唯一输入，替换旧的静态原型，建立后续关卡可复用的固定步长战斗核心、WebGL Spine 渲染层和 React HUD 边界。

## 已实现

- 从 `LevelAssetManifest` 加载 12 个路径点、12 个塔位、4 名英雄和 6 波 18 个刷怪组；
- 使用固定 60Hz 模拟，RAF 只负责累积时间和渲染，React 每约 100ms 接收一次 HUD 快照；
- 页面隐藏、暂停和结算状态不补算战斗时间，支持一倍速与二倍速；
- 怪物按真实路径弧长移动，到达终点后按配置扣除水晶生命；
- 英雄按原配置的基础攻击、伤害系数、冷却和范围选择最前目标；
- 仅法师和炮手在已确认外部事件帧结算伤害；骑士和游侠只播放攻击动画；
- 使用 900x1600 逻辑坐标和原版第一关地图、部署石台、BGM；
- WebGL 加载并逐帧渲染 4 名英雄和 3 类怪物的 Spine 4.2.33 资源；
- 单位按脚底纵坐标排序，2D overlay 绘制怪物生命条；
- 提供波次、剩余敌人、水晶生命、部署数量、战斗时间、暂停、倍速和胜负返回；
- 结算成功后才写入第一关星级与最佳水晶生命存档；
- 独立回归入口为 `/frontline-battle`，不依赖桌面窗口操作。

## 数据边界

- 英雄配置链为 `hero_c -> PetSkill -> Skill -> Freeze`，基础攻击来自 `HeroPropBase`；
- 第一关 Manifest SHA-256：
  `c4a3dfad905c5cebf4ab38c77e443e49f936ac332f357a0f6ff970939bbde029`；
- 波次配置 SHA-256：
  `73e8b14d27ff4f44ca1f4c81c8c3960db92fa13bf0e84d9ad6fd103419349bbf`；
- 当前绝对怪物生命仍未从 `MonsterLevelProp_C.csv` 恢复，运行模型暂为
  `100 * lHP_Count`；
- 原版怪物 Spine 不含 `hurt` 动画，本轮未伪造骨骼受击动作；
- 未确认命中帧、投射物和特效归属的能力不会静默补值。

## 本轮修正

`EctypeWave.waitTimeMs=0` 表示禁用最大等待时间。战斗核心曾把零值当成已超时，
导致第一波最后一只怪生成后立即切波；现已改为仅在等待时间大于零时启用超时分支，
并增加固定 60Hz 回归用例。

## 验证

- [x] 两次完整资源审计生成得到相同 Manifest 和波次配置哈希
- [x] TypeScript 检查通过
- [x] 3 个 Frontline 测试文件共 18 项测试通过
- [x] Python 审计与格式解析脚本编译通过
- [x] TRAE 浏览器回归通过：约 19 秒仍为第 1 波，两个 Canvas 非空且控制台无错误
- [x] `git diff --check` 通过
- [x] 未运行 lint
- [x] 未运行完整构建

## 后续验收项

- 恢复 `MonsterLevelProp_C.csv` 中的绝对怪物生命与等级属性；
- 锁定骑士和游侠普通攻击命中时点；
- 提取并渲染法师、炮手投射物及命中特效；
- 依据原版素材补充受击材质反馈、召唤、强化、主动技能和结算演出；
- 按 Spec 的视觉对照节点完成第一关逐帧验收。
