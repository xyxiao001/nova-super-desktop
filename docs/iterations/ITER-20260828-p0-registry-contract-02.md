# ITER-20260828：增加应用清单一致性测试

- 状态：已完成
- 所属 Spec：`docs/specs/SPEC-20260828-p0-platform-foundation.md`
- 对应验收项：AC-2、AC-3、AC-7、AC-8
- 开始日期：2026-08-28
- 完成日期：2026-08-28

## 本轮目标

让测试直接保护应用注册表、动态加载清单和游戏目录之间的 ID 一致性。

## 本轮不做

- 不修改任何清单内容、应用入口或加载路径。

## 计划修改

- `tests/unit/appRegistry.test.ts`：增加跨清单契约；
- `tests/unit/lazyApps.test.ts`：移除重复固定快照，保留 loader 能力测试。

## 实现假设

直接比较运行时清单集合比手写完整 ID 快照更能防止新增应用漏接。

## 实现—验证记录

### 第 1 轮

- 实现：增加注册表与动态 loader 的 ID 集合比较，并验证全部游戏目录项均为已注册的非 launcher 窗口；移除重复的固定完整 ID 快照；
- 验证：执行两个定向测试文件、全部单测和 TypeScript 检查；
- 结果：定向测试 5 项通过；全部 22 个测试文件、98 项测试通过；TypeScript 检查通过。

## 验证结果

- [x] `npx tsc --noEmit`
- [x] 相关单测
- [x] 全部单测
- [x] 未运行 lint
- [x] 未运行完整构建

## Diff 审计

- [x] 所有修改都对应本轮目标
- [x] 没有未授权的兜底、校验、兼容或重构
- [x] Spec 的不修改边界保持不变
- [x] 没有覆盖用户原有修改
- [x] `git diff --check` 通过

## 交付记录

- 完成结果：应用注册、动态加载和游戏目录的一致性由测试直接保护；
- 用户可感知变化：无；
- 未实现能力：无；
- 新增或更新 ADR：无；
- 回写 Spec 的后续事项：无。
