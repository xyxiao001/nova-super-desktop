# ITER-20260902：窗口实例化验收与产品说明

- 状态：待浏览器验收
- 所属 Spec：`docs/specs/SPEC-20260902-window-instances.md`
- 对应验收项：AC-1 至 AC-14
- 开始日期：2026-09-02
- 完成日期：

## 本轮目标

核对窗口实例化的全部验收项，同步 README 产品说明，并完成类型检查、相关单测和范围审计。

## 本轮不做

- 不新增窗口实例能力；
- 不启动未经用户授权的开发服务器；
- 不实现跨窗口拖放、照片实验室多实例或会话恢复；
- 不修改桌面文件、备份、资源缓存或 Service Worker；
- 不运行 lint 或完整构建。

## 实现—验证记录

### 第 1 轮

- 实现：完成第 4 轮记录，更新 README 的多窗口、任务栏分组、移动端和快捷键说明；同步 Spec 的实际文件名和验收状态；
- 验证：审计窗口实例、资源目标、任务栏分组、移动端样式、文件生命周期、懒加载和持久化代码路径；
- 结果：AC-1 至 AC-13 已由实现、定向测试和静态审计覆盖；AC-14 的 TypeScript、相关单测和 diff 检查已完成，浏览器关键路径尚未执行。

### 第 2 轮

- 实现：修复临时实例自动偏移和贴靠几何覆盖应用级窗口模板的问题；几何模板现在只在用户拖动、缩放或居中窗口时更新；
- 验证：增加实例几何偏移纯函数测试，执行 TypeScript 和完整窗口实例相关测试集；
- 结果：TypeScript 通过，13 个测试文件共 73 项测试通过，`git diff --check` 通过。

## 验证结果

- [x] `npx tsc --noEmit`
- [x] 13 个相关测试文件、73 项测试通过
- [x] `git diff --check`
- [x] README 已同步当前多窗口能力
- [ ] 桌面和移动端最短浏览器关键路径
- [x] 未启动开发服务器
- [x] 未运行 lint
- [x] 未运行完整构建

## Diff 审计

- [x] 所有修改都对应窗口实例化目标
- [x] 没有新增依赖、持久化结构或 Service Worker 行为
- [x] 单例应用和按需加载边界保持不变
- [x] 没有覆盖用户原有修改
- [x] `git diff --check` 通过

## 交付记录

- 完成结果：窗口实例化实现、自动化验证、文档和范围审计已完成；
- 用户可感知变化：资源管理器可显式新建窗口，文件夹、文稿和桌面图片可并行打开，任务栏按应用分组管理实例，移动端继续使用单实例全屏体验；
- 待验收项：需在获准启动开发环境后执行一条桌面和移动端浏览器关键路径；
- 未实现能力：跨窗口拖放、照片实验室多实例和会话恢复；
- 新增或更新 ADR：沿用 `ADR-0006-window-instance-model.md`。
