# SPEC-20260904：首关攻击闭环对齐

- 状态：待执行；本次仅编写 Spec，未实施下述功能。
- 提出日期：2026-09-04
- 负责人：Codex
- 代码基线：`be94569`（已推送 `main`）
- 关卡：`100101`，1-1 烈日沙漠1，已完成教学账号重玩。
- 参考资源版本：`412f11e3c27d645ddeafcf921f558d57`
- 上位规划：[高保真战斗复刻](./SPEC-20260903-frontline-faithful-battle.md)
- 行为依据：[实机流程与差异](../reverse-engineering/frontline-rkt/LEVEL_100101_PLAYTEST.md)
- 技术路线依据：[原版运行时验证](../reverse-engineering/frontline-rkt/NATIVE_RUNTIME_PROBE.md)

## 背景

首关已经接入原版地图、Spine、波次、领主移动、拖动合成及简化攻击。当前仍把远程出手事件近似为直接扣血，闪电用短暂直线表达，小丑皇毒区及精灵大师移动召唤物尚未实现，不能按完整战斗验收。

原版两段 WASM 能编译，但框架启动被微信 SDK 依赖阻断，尚未进入实例化和场景。本阶段采用“读取原版 Lua、配置和资源，移植已证实行为，再与实机对照”的路线。完整微信运行时移植不作为本阶段前置条件。

本 Spec 是下一阶段的有限执行范围，不启动上位规划中的自动逐关扩展。历史实机记录按时间追加，其中早期的“合成手势未知”和“固定教学召唤”已被后续证据及提交取代。

## 目标

在首关完成四名出战英雄与沙王的基础攻击闭环：索敌 → 起手 → 发射或效果生成 → 命中 → 伤害/状态结算 → 收招及下一次攻击。每个环节有可追溯依据和独立验收结果，明确区分动画事件、投射物命中、技能等待时间与冷却。

先完成闪电丘的闭环，作为首个可对照交付，再逐个接入暴走萌弹、小丑皇、精灵大师和沙王。每个执行回合只处理一个明确问题；上一项通过后进入下一项。

## 非目标

- 新关卡、新英雄、养成、战斗 HUD 重做、主动技能、经济调整和结算改版。
- 怪物完整队形与寻路、领主整条移动路径、合成权重和升阶成长公式；仅观察其对当前攻击样本的影响。
- 完整 Unity/微信 SDK 移植、游戏账号、服务器、远端存储或付费链路。
- 通用战斗框架重构、新增备用渲染器、自动重试、资源占位或静默降级。
- 自动提交、推送、部署或发布；这些操作按用户后续指令执行。

## 当前行为与证据边界

| 对象 | 当前实现 | 下一步必须核对 |
| --- | --- | --- |
| 闪电丘 | 基础射程280逻辑像素，基础冷却1.6秒；起手后0.233333秒直接结算主目标及两次连锁 | 该时间是否只是发射；弹道速度；连锁搜索中心、优先级、逐跳时序 |
| 暴走萌弹 | 基础射程340像素，基础冷却0.8秒；起手后0.033333秒直接扣血 | 发射事件、弹体、飞行与命中时机，技能等待时间与动画结束关系 |
| 小丑皇 | 基础射程320像素，基础冷却3秒；当前配置直接伤害系数为0 | 弹体到达后触发的技能段、毒区范围/存续/伤害周期和归属 |
| 精灵大师 | 基础射程210像素，基础冷却20秒；命中时间为空 | 召唤触发、笑笑鬼属性、上限、移动接敌、嘲讽、死亡与重新召唤 |
| 沙王 | 基础射程已由400修正为40像素，移动速度200像素/秒；现用0.066667秒扣血 | 最终射程是否计入碰撞半径或账号增益；命中事件、范围、攻击系数及移动打断 |

上述数值描述提交基线，不代表所有数值均已通过实机验收。具体注意：

- 属性5以毫米表示；当前投影每世界单位100逻辑像素。基础属性、最终有效射程和屏幕范围圈必须分开记录。
- Spine `attack_1` 时长不必然等于 `Skill.WaitTime` 或冷却；事件可能仅播放音效或生成弹体。
- 闪电连锁的额外2个目标、75%伤害有实机技能说明；现用最近目标逐跳搜索仍待确认。
- 原弹体 `destroyDelayTime=0.15` 不证明飞行耗时为0.15秒，也不证明所有连线持续0.15秒。
- 小丑皇直接伤害系数0不能据此改成任意非零值；精灵大师统计为0不能据此认定召唤物无伤害。
- 合成同类同阶后从出战池随机变成高一阶英雄已有用户确认；等概率、四阶上限和升阶属性尚未作为原版规则验收。

## 执行协议

### 1. 建立可重现的攻击样本

在现有逆向文档目录新增 `ATTACK_100101_EVIDENCE.md`，每种攻击使用独立样本编号。记录：

| 字段 | 要求 |
| --- | --- |
| 环境 | 资源版本、关卡、x1/x2、可见账号等级/装备/增益；未知项明确标记 |
| 单位 | 英雄ID、等级、合成阶、台座或领主位置、目标类型与站位 |
| 静态证据 | 配置表主键、字段、Lua函数、Bundle对象、动画事件或外部时序资源 |
| 动态证据 | 原版视频/截图路径、采样帧率、片段起止时间、操作步骤 |
| 时间线 | 首次起手、发射、首次命中、连锁各跳、收招、下一次起手 |
| 判定 | confirmed / inferred / unknown，冲突与未排除变量 |

优先读取已提取数据；只针对源码不能解释的行为做短片段实机采样。账号状态不一致时不能用伤害差直接推导公式。截图左上角整数秒不足以验证攻击关键帧。

`/tmp/frontline-rkt-recovered` 和 `/tmp/frontline-wxapp` 是临时目录：证据记录应包含原文件标识、版本、提取命令或输入来源及必要哈希。目录缺失时按现有提取流程恢复，不能在生产代码依赖这些绝对路径，也不提交整个客户端缓存。

### 2. 锁定本次最小改动

每次实现前，在证据文档写出一个可重现偏差、支持结论的依据、涉及文件和验收样本。只将 confirmed 的字段用于替换现有近似行为；未知行为不补猜测默认值。

若关键参数缺失，先补取证；无法取得则记录具体缺失项，保持该项未完成，继续能独立完成的工作。确实需要用户提供画面或数据时，提出具体采样请求。Spec 本身不构成超范围兜底、校验或兼容分支的授权，相关扩展仍遵守 AGENTS.md。

### 3. 实现与验证

每项最多三轮实现—验证。默认一轮定位、一轮实现、一轮验证；仅失败时追加一轮。不得用反复全关游玩替代定点验证，不能把测试通过写成视觉1:1通过。

## 技术方案

### 配置与提取

- 在现有提取器和关卡 Manifest 中按证据补充攻击需要的字段，运行时消费单一数据来源。不要另建一套与 Manifest 漂移的技能常量表。
- 分别表达有证据的发射事件、命中机制、技能锁定时长、冷却及特效寿命；不把这些概念继续全部塞进 `hitTimeSeconds`。
- 字段的具体结构由首个已证实攻击样本决定，只添加当前技能实际需要的数据，不预建全部技能系统。
- 修改提取数据时同步对应类型、正式产物及直接相关资源契约测试。无需为未变化的资源重新生成全包。

### 模拟与表现

- `frontlineCore.ts` 保有战斗结果的唯一决定权；动画播放或渲染回调不再次扣血。
- 若已确认存在飞行过程，则在 `BattleState` 中记录其发射和在途状态，按模拟时间推进，到达时按原规则命中；具体跟踪/落点/目标死亡规则先取证。
- `BattleCanvas.tsx` 使用模拟中的同一攻击时间线播放 Spine、弹体和命中特效，避免渲染帧率改变命中顺序。
- 原资源中已有的相关贴图、骨骼、音效和可提取特效参数优先复用；Unity特效需移植表现，不能把临时直线命中提示当作最终效果。
- 毒区及召唤物仅在各自迭代增加所需状态；它们的伤害和击杀归属接入现有结算，不额外发放奖励。
- 暂停冻结所有新增模拟与表现时间；倍速按既有模拟比例推进。关闭窗口继续遵守现有应用生命周期。

### 状态归属

战斗状态留在游戏内部，配置随打开游戏按需加载。投射物、毒区、召唤物和攻击事件是单局临时状态；不新增存档字段、IndexedDB、localStorage、云端数据或迁移。

## 迭代拆分与交付

| 顺序 | 单目标 | 实施前必须回答 | 交付 / 完成条件 |
| --- | --- | --- | --- |
| A0 | 锁定闪电丘攻击基线 | 0.233333秒对应什么事件？技能如何触发命中？ | 首个完整时间线、来源映射与差异列表，关键未知项已列明 |
| A1 | 对齐闪电丘主目标攻击 | 弹体是否飞行、如何命中、何时允许下次起手？ | 单目标连续三次攻击的模拟与表现对照通过 |
| A2 | 对齐闪电丘连锁 | 搜索中心/半径/顺序、每跳时间、伤害基数是什么？ | 三目标和范围外目标样本通过，替换已证伪的连线/连锁近似 |
| A3 | 对齐暴走萌弹普攻 | 发射与命中时差、弹体轨迹、收招和冷却规则是什么？ | 复用已需要的攻击能力完成该英雄对照，避免复制整套核心 |
| A4 | 对齐小丑皇投弹与毒区 | 子技能、落点、半径、持续时间、周期与叠加规则是什么？ | 一次投弹从生成到毒区结束的伤害与表现记录通过 |
| A5 | 对齐精灵大师召唤攻击 | 生成时点、继承属性、上限、嘲讽/接敌和重召规则是什么？ | 一次召唤至接敌攻击的闭环通过，上限/死亡重召按已证实规则测试 |
| A6 | 对齐沙王近战攻击 | 碰撞半径、最终距离、出手、伤害和移动打断如何作用？ | 范围外不打、进入后攻击、移动中断及停止后再攻击对照通过 |
| A7 | 首关攻击联合验收 | 单项组合是否影响时序、重复伤害或结算？ | 四英雄与沙王同场的局部战斗对照及一次首关通关记录 |

A4、A5如涉及多个独立机制，继续拆成“弹体/毒区”或“召唤/接敌/嘲讽”的单目标回合。不能为了同次交付越过证据缺口。A7不代表怪物、经济、合成成长、HUD和整关视觉已全部验收。

## 修改范围

| 文件 | 允许的修改目的 |
| --- | --- |
| `scripts/frontline/audit_level_01.py` 及确有需要的现有提取脚本 | 提取经确认的攻击配置、时序与相关资源 |
| `src/apps/frontline/levelAssetManifest.ts` | 表达已实现攻击所需的配置类型 |
| `public/assets/games/frontline/levels/desert-1/` | 更新关联Manifest和配置产物 |
| `public/assets/games/frontline/` 对应角色/特效子目录 | 仅新增当前攻击使用的原资源及来源记录 |
| `src/apps/frontline/frontlineLevel.ts` | 将配置接入战斗 |
| `src/apps/frontline/frontlineCore.ts` | 索敌、攻击时间线、命中及必要的技能状态 |
| `src/apps/frontline/BattleCanvas.tsx`、`frontlineSpine.ts` | 与同一模拟时间线同步的攻击表现 |
| `tests/unit/frontlineCore.test.ts`、`frontlineLevel.test.ts`、`frontlineLevelAssetManifest.test.ts` | 直接相关行为与数据契约测试 |
| `docs/reverse-engineering/frontline-rkt/ATTACK_100101_EVIDENCE.md` | 样本、参数来源、误差及未完成项 |

不改桌面外壳、应用注册、其他游戏、存档、招募或养成页面。不顺手重构 `Core`，不修改经济、合成随机分布或阶级成长。若新技能必须影响怪物接敌等行为，将该依赖作为可审阅的单目标先说明并取证。

## 平台影响

| 检查项 | 本阶段影响 |
| --- | --- |
| 应用懒加载、窗口、跨应用事件 | 保持现状 |
| 本地数据、备份、清理 | 无新增数据或迁移 |
| 资源包、离线 | 攻击资源沿用现有frontline资源包和按需加载；不提前加载至桌面壳 |
| Worker / iframe / WASM | 不新增运行时或宿主 |
| 构建与部署 | 默认不构建、不发布；后续发布时遵守现有Service Worker版本流程 |

## 验收标准

- [ ] AC-1：五类单位均有可追溯攻击时间线；未确认字段没有作为新业务常量写入。
- [ ] AC-2：同一静止站位下，射程内外及目标选择与原版样本一致；碰撞和增益变量有记录。
- [ ] AC-3：发射、命中、伤害和连锁不再凭同一“出手帧”推定；每个已确认事件按其真实机制触发且每次只结算一次。
- [ ] AC-4：连续三次攻击的起手间隔、收招与冷却关系匹配样本；移动打断按沙王原规则处理。
- [ ] AC-5：闪电数量/顺序/比例、毒区周期/存续、召唤物上限/属性/归属分别通过对应样本。
- [ ] AC-6：关键事件误差不超过上位Spec的80ms；900×1600画布相关锚点误差不超过6px、角色比例误差不超过5%。采样不足以判断时标记未验收，不能放宽标准后宣称通过。
- [ ] AC-7：固定输入下不同渲染帧率不改变命中顺序；暂停不推进新增状态，x2按模拟时间同比加速。
- [ ] AC-8：没有重复击杀收益或攻击状态串入下一局；既有召唤/合成/经济流程没有被本阶段改写。
- [ ] AC-9：相关TypeScript、单测和最短交互通过；首关联合样本及剩余差异有记录。
- [ ] AC-10：交付说明只对通过的单项声明完成，不将简化特效或单测通过表述为完整1:1。

## 验证计划

每次代码改动完成后运行一次：

```sh
npx tsc --noEmit --incremental false
npx vitest run tests/unit/frontlineCore.test.ts tests/unit/frontlineLevel.test.ts
```

只有资源或Manifest变更才追加 `tests/unit/frontlineLevelAssetManifest.test.ts`。单测围绕真实行为风险：发射前不命中、在途目标变化、重复结算、时间推进、状态到期及技能特定边界。原版尚未确定的边界先取证，不用测试固化猜测。

浏览器每项走一条最短路径：本地战斗入口 → 首关 → 对应英雄及台座 → 目标事件 → 暂停采样。优先现有入口和测试夹具；测试中固定阵容/随机值不得改变生产召唤规则。A7再做一次组合通关，不为每个小修反复全关操作。

保存对齐时间基准的原版/本地片段和必要叠图，列出起手、发射、命中的误差。人工操作参考客户端与自动操作混入同一片段时单独标记，不能误记为脚本输入。

文档改动只检查内容与链接，不运行代码测试。禁止默认运行lint、完整构建、全量测试和无明确问题的重复回归。检查目标diff并排除临时录像、浏览器测试目录、覆盖率和tsbuildinfo等产物；必要证据按清单保存，不批量提交缓存。

## 风险、阻塞与回滚

- 引擎桥接内的规则可能不在Lua中：静态证据不足时需要定点实测，仍不足则记录阻塞，不能伪造等价结论。
- 参考客户端版本或账号状态变化会使比较失效：注明差异，重新取得该项可比样本，不混用版本数值。
- 新攻击能力可以按A1–A6分别回滚；保留`be94569`的已完成合成、移动和距离修正，不回退其他独立工作。
- 无数据迁移。任何超过本Spec边界的兼容/兜底/校验扩展按AGENTS.md处理。

## 后续阶段队列

1. 怪物走势：出怪节奏、横向错位、速度、接敌停留和水晶抵达。
2. 合成完整表现：升阶属性、外观、范围提示与实际上限；随机权重另取证。
3. 首关经济、HUD、主动技能及结算视觉对齐。

这些项目仍属于整关复刻目标，但不随本Spec自动实施。第一关未完成联合验收前不进入第二关。

## 确认记录

- 2026-09-04：用户要求“建立一个下一阶段的执行spec”；本文件落实下一阶段计划，所有执行及验收复选项保持未完成。
